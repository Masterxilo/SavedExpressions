System`HoldComplete[Global`RedefinePublicFunction[
  Global`SOPMakeFy[Global`p_Global`SparseOptimizationProblem], "F(x) is the \
concatenation of all individual f, evaluated as f(select_p(x)). F(y) only \
takes the y data, taking the rest of x as constant.\n\nThis creates a pure \
function that emulates F(y)", 
  Global`SOPFx[Global`p, Global`SOPMakeXWithY[Global`p, #1]] & , _, ""]]
